
<% salads.forEach((row) => { %>
      <%= row['name'] %>
      <%= row['price']%>
    <% }) %>
<% Wraps.forEach((row) => { %>
      <%= row %>
      <% }) %>
<% Soups.forEach((row) => { %>
      <%= row %>
      <% }) %>


<% salads.forEach((row) => { %>
      <%= row['name'] %>
      <%= row['price']%>
      <%= row['description']%>
      <img src='<%=row['pictures'] %>'>
      <%= row['likes']%>
      <button>Add</button>
    <% }) %>
<% Wraps.forEach((row) => { %>
      <%= row['name'] %>
      <%= row['price']%>
      <%= row['description']%>
      <img src='<%=row['pictures'] %>'>
      <%= row['likes']%>
      <button>Add</button>
      <% }) %>
<% Soups.forEach((row) => { %>
      <%= row['name'] %>
      <%= row['price']%>
      <%= row['description']%>
      <img src='<%=row['pictures'] %>'>
      <%= row['likes']%>
      <button>Add</button>
      <% }) %>

<section class='menu col-12 col-md-8'>
  <section class = "row">
    <% salads.forEach((row) => { %>
          <%= row['name'] %>
          <a> price $ <%= row['price']%> </a>
          <%= row['description']%>
          <img class = "food-image" src='<%=row['pictures'] %>'>
          <%= row['likes']%>
          <button>Add</button><br>
        <% }) %>
  </section>
  <section class = "row">
    <% Wraps.forEach((row) => { %>
          <%= row['name'] %>
         <a> price $ <%= row['price']%> </a>
          <%= row['description']%>
          <img class = "food-image src='<%=row['pictures'] %>'>
          <%= row['likes']%>
          <button>Add</button><br>
          <% }) %>
  </section>
  <section class = "row">
    <% Soups.forEach((row) => { %>
          <%= row['name'] %>
          <a> price $ <%= row['price']%> </a>
          <%= row['description']%>
          <img class = "food-image" src='<%=row['pictures'] %>'>
          <%= row['likes']%>
          <button>Add</button><br>
          <% }) %>
  </section>
</section>
